DELIMITER //

CREATE FUNCTION is_palindrome(str VARCHAR(225)) 
RETURNS BOOLEAN 
NO SQL
BEGIN 
    SET str = REPLACE(str, ',', ''); 
    SET str = LOWER(str);           
    RETURN str = REVERSE(str);      
END //

DELIMITER ;

-- To test the function:
SELECT is_palindrome('mam') AS palindrome;
